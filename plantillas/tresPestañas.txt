private void selectTab(int position) {
        // Cambiar colores de las pestañas
        resetTabColors();
        
        // Ocultar todos los contenidos
        content1.setVisibility(View.GONE);
        content2.setVisibility(View.GONE);
        content3.setVisibility(View.GONE);
        
        // Mostrar contenido seleccionado y cambiar color
        switch (position) {
            case 0:
                tab1.setTextColor(getResources().getColor(android.R.color.holo_red_dark));
                content1.setVisibility(View.VISIBLE);
                break;
            case 1:
                tab2.setTextColor(getResources().getColor(android.R.color.holo_red_dark));
                content2.setVisibility(View.VISIBLE);
                break;
            case 2:
                tab3.setTextColor(getResources().getColor(android.R.color.holo_red_dark));
                content3.setVisibility(View.VISIBLE);
                break;
        }
        
        // Mover el indicador
        moveIndicator(position);
______________________________
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!-- Pestañas -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#FFFFFF"
        android:padding="0dp">

        <TextView
            android:id="@+id/tab1"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:background="#FFFFFF"
            android:gravity="center"
            android:text="OPCIÓN 1"
            android:textColor="#FF6B6B"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tab2"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:text="OPCIÓN 2"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:background="#FFFFFF"
            android:textColor="#666666"/>

        <TextView
            android:id="@+id/tab3"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:text="OPCIÓN 3"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:background="#FFFFFF"
            android:textColor="#666666"/>
    </LinearLayout>

    <!-- Indicador (línea debajo de la pestaña activa) -->
    <View
        android:id="@+id/indicator"
        android:layout_width="0dp"
        android:layout_height="3dp"
        android:background="#FF6B6B"/>

    <!-- Contenedor de los fragmentos -->
    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</LinearLayout>
    }